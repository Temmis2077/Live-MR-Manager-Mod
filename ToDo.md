# 🗺️ Live MR Manager — Mod 로드맵 (ToDo)

이 문서는 **개조(모드) 레포의 로드맵**입니다. 원본 프로젝트의 로드맵은 [업스트림 ToDo](https://github.com/AutumnColor77/Live-MR-Manager/blob/main/ToDo.md)를 참고하세요.

## 🤖 1. AI 가사 자동 정렬 (핵심 축)

- [x] CTC 강제정렬 엔진 (Rust + ONNX Runtime, Viterbi) — 보컬 스템 우선 사용
- [x] 한국어 정렬 모델 변환·배포 (kresnik/wav2vec2-large-xlsr-korean → ONNX, 전용 Release)
- [x] 영어(팝송) 정렬 모델 추가 (facebook/wav2vec2-base-960h) + 정렬 언어 토글
- [x] 랩/혼합(한+영) 듀얼 모델 정렬 — 줄별 우세 언어 병합
- [x] 문장부호·숫자·특수기호 자동 필터 (토크나이저 + 결과 병합 정규화)
- [x] 정렬 대기열 — 라이브러리 다중 선택 일괄 정렬, 진행률/취소, 모델 로드 "준비 중" 표시
- [x] 정렬 모델 인앱 다운로드 배관
- [x] **정렬 전용 디리버브** — 분리된 보컬의 잔향/이펙트를 걷어낸 보컬로 정렬(정렬할 때만 실행·캐시, 재생엔 미적용). 리버브가 심하면 정확도가 크게 떨어지는 문제 대응. GPU 가속 팩 필요, 없거나 실패하면 원본 보컬로 폴백 → [docs/DEREVERB.md](docs/DEREVERB.md)
- [x] **괄호 코러스 가사 보존** — `(사랑해)`처럼 실제로 불리는 코러스를 정렬 대상에서 통째로 지워 그 뒤 전부 밀리던 문제 수정(곡 구조 지시어 `[Chorus]`만 제거)
- [x] **한/영 혼합곡 밀림 수정** — 한쪽 언어 모델 패스가 다른 언어 줄 전체를 만나면 그 구간 시간이 통째로 무시돼 뒤가 밀리던 문제
- [ ] 영어→한글 발음 치환 — 한 줄 안 한·영 혼합 정밀 정렬 (CMU 발음사전 방식, **조사 완료·보류**)
- [ ] 보컬로이드 등 합성 음성 인식률 개선 (현재는 안내 문구로 대응)
- [ ] **코러스 분리 모델** (가사싱크 전용, 사용자 트리거) — 가사에 코러스 표기가 아예 없는 곡 대응. 위 괄호 수정으로 얼마나 남는지 보고 판단
- [ ] **BPM 앵커 기반 구간 정렬** — 현재는 곡 전체를 한 번에 순차 정렬해서 중간에 한 번 어긋나면 복구 지점이 없다. 박자로 앵커를 잡아 구간별로 정렬하면 밀림에 훨씬 견고해짐 (정렬 엔진 구조 변경, 큰 작업)

## 📝 2. 가사 싱크 편집기

- [x] 3줄(원문/차음/번역) 가사 모드 — 일본어 곡 대응, 표시 항목 스코프별 토글
- [x] 보컬 시작/간주 마커 + 파형 자동 감지
- [x] MV 인트로 자동 감지 (AI 정렬 첫 줄 기준, 원클릭 적용)
- [x] 인트로 자동 건너뛰기 — 간주(전주) 마킹 시 음악 시작부터 재생
- [x] BPM 그리드 러프 배치
- [x] 가사 싱크 상태 분류(싱크 완료/미싱크/가사 없음) + 라이브러리 필터·음원 선택 모달 그룹화
- [x] 재생 곡 자동 추적 (가사 싱크 탭이 열려 있으면 재생 곡 따라감)
- [x] 가사 순서 뒤섞임 버그 수정 (저장 시 세그먼트 순서 보존)
- [x] **파형 블럭에 가사 표시** — 어느 블럭이 어느 줄인지 파형만 보고 파악
- [x] **가사 블럭 타겟 선택** — 목록 클릭 시 고정, 파형·플레이바·재생 진행 시 그 시각의 블럭으로 자동 추적(간주 구간에선 선택 유지)
- [ ] **곡별 가사 싱크 데이터 공유** — 타임코드+블록 수만 저장, 가사 원문 미저장(저작권 회피). 후순위·설계 필요

## 🔧 3. 편의 기능

- [x] MR 분리 방식 선택 모달 (빠른 분리/고품질 분리) + 곡별 사용 모델 기록
- [x] MR 캐시 저장 위치 설정 — 멀티 PC 분리 결과 공유
- [x] 플레이어 컨트롤바 빠른 메뉴(⋮) — 곡 정보 수정/MR 분리/가사 싱크 열기
- [x] Meloming 가사 자동 .lrc 시드, 가사 링크 노출
- [x] **곡 추가 전용 UI** — 유튜브·가사 사이트 통합 검색(공식 음원 우선), 다중 추가, 곡 정보·장르·카테고리·분리 모델을 한 화면에서 설정
- [x] **제목/가수 자동 추정** — `아티스트 - 곡제목 (Official MV)` 류 패턴을 파싱해 프리필(영상 제목·업로더 그대로 쓰던 것 개선)
- [x] 장르/카테고리 2축 표준화 + 기존 데이터 자동 재매핑 → [docs/GENRE_CATEGORY_STANDARD.md](docs/GENRE_CATEGORY_STANDARD.md)
- [x] 대기열 영속화(앱 재시작 후 복원·재개) + 유튜브 실패 시 지수 백오프 재시도
- [x] MR 폴더 열기 (플레이어 ⋮ 메뉴) — 캐시 폴더명이 URL 인코딩이라 직접 찾기 어려운 문제 해소
- [ ] 재생 큐 / 반복·셔플 (업스트림 로드맵과 조율)

## 🔊 4. 오디오 엔진 (스트리머 실전용)

- [x] **실시간 출력 장치 전환** — 재생 중에도 즉시 전환(위치·볼륨·피치/템포 유지), 선택 저장·복원
- [x] **트랙 믹서** — 보컬/MR 독립 페이더 + 음소거·솔로 (볼륨·밸런스 위에 곱해지는 트림)
- [x] **모니터 / MR 이중 채널** — 채널마다 다른 출력 장치, 채널×소스(보컬·인스트·메트로놈) 2×3 라우팅 매트릭스. MR 채널 미설정 시 기존 단일 출력과 100% 동일
- [x] **메트로놈** — BPM 클릭(4박 다운비트 강조), 탭 템포(자동 탐지 BPM에서 시작해 탭으로 보정), 재생 중 BPM 실시간 반영
- [x] **버스별 지연 보정** — 방송/녹음에서 목소리와 MR 정렬용. 장치 버퍼 기반 추정값을 시작점으로 제공
- [x] **출력 소프트 리미터** — 여러 소스 합산 시 클리핑 방지(임계값 이하는 투명, 이상은 부드럽게 포화)
- [x] **MR 분리 GPU 가속(TensorRT)** — 곡당 20~30분 → 약 30초 → [docs/GPU_ACCELERATION.md](docs/GPU_ACCELERATION.md)
- [x] 커스텀 분리 모델 — 가이드 + 추천 모델 원클릭 추가 → [docs/CUSTOM_MODELS.md](docs/CUSTOM_MODELS.md)
- [ ] 첫 가사 진입 박자 카운트 (메트로놈 카운트인)
- [ ] A-B 루프(구간 반복) — 연습 방향의 킬러 기능
- [ ] 버스별 피크/RMS 미터(VU)
- [ ] 웹 리모컨 (OBS 독)

## 🔀 5. 배포 · 업스트림

- [x] 모드 자체 베타 릴리즈 (`v0.6.0-beta.1` ~ `beta.4`)
- [x] 자체 업데이트 알림 — 원본이 아닌 이 레포의 릴리즈를 보도록 전환(베타 → 다음 베타로 이어짐)
- [x] Draft PR #1 — 가사 싱크·AI 정렬 기능 일괄 정리 (이 레포 `main`에 병합됨)
- [ ] 완성도 확인된 기능을 업스트림에 제안
- [ ] GPU 가속 팩 배포 방식 확정 — cuDNN/TensorRT 재배포 라이선스 검토 필요(현재는 수동 설치 안내)

## 🧪 6. 장기 구상

- [ ] **소형 DAW 확장** — 내부 채널 이펙트(MR 리버브), 마이크 입력 + 이펙트, VST 호스트. 앱 베이스가 완성된 뒤의 이야기. 현재 오디오 엔진(rodio)은 재생 엔진이라, 이 방향으로 가려면 cpal 기반 블록 처리 믹서 그래프로 이전이 필요 — 그때까지 rodio에 DSP를 붙여 잠금(lock-in)을 만들지 말 것
- [ ] 악기 분리 모델 기반 **연습용 버전**
