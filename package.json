{
  "name": "tauri-app",
  "private": true,
  "version": "0.6.0-beta.2",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "node scripts/dev-server.mjs --root src --port 1420",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vitest": "^3.2.4"
  }
}
