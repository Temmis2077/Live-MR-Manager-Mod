# OAuth Redirect URI (미설정 시 요청 Origin + /oauth/callback)
# 프로덕션: https://lmrm.vercel.app/oauth/callback
# NEXT_PUBLIC_OAUTH_REDIRECT_URI=https://lmrm.vercel.app/oauth/callback

# OAuth 토큰 교환·웹 로그인 (Vercel 서버 전용 — Git 커밋 금지)
MELOMING_CLIENT_ID=
MELOMING_CLIENT_SECRET=
